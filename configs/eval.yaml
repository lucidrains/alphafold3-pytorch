# @package _global_

defaults:
  - _self_
  - data: atom # choose datamodule with `test_dataloader()` for evaluation
  - model: alphafold3
  - logger: csv
  - strategy: default
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - environment: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
